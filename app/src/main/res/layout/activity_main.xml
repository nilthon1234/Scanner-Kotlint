<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Cámara en pantalla completa -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Marco del escáner -->
    <View
        android:id="@+id/overlayFrame"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:background="@drawable/border_frame"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.4" /> <!-- MÁS ARRIBA -->

    <!-- Línea animada -->
    <View
        android:id="@+id/laserLine"
        android:layout_width="200dp"
        android:layout_height="2dp"
        android:background="#00FF00"
        app:layout_constraintTop_toTopOf="@id/overlayFrame"
        app:layout_constraintStart_toStartOf="@id/overlayFrame"
        app:layout_constraintEnd_toEndOf="@id/overlayFrame" />

    <!-- Mensaje de instrucciones -->
    <TextView
        android:id="@+id/instructionText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enfoque el QR dentro del cuadro"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/overlayFrame"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
